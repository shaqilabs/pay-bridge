# Examples

Run any example from the project root:

```bash
composer install
php examples/AbhiPay.php
```

Most examples use placeholder credentials; replace them with real sandbox/production values before running.

